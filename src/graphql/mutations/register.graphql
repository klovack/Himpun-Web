mutation Register($credentials: UsernamePasswordInput!, $options: NameInput){
  register(
    credentials: $credentials
    options: $options
  ) {
    errors {
      field
      message
    }
    user {
      ...CompleteUser
    }
  }
}